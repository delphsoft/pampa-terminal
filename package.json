{
  "name": "pampa-terminal",
  "version": "1.0.0",
  "description": "Terminal profesional de análisis de mercado argentino",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "lint": "echo 'No linter configured'"
  },
  "engines": {
    "node": ">=18.x"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  }
}
